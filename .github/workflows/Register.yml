name: Register
on:
  workflow_run:
    workflows: ["Test"]
    types: [requested]
    branches:
      - "main"
  workflow_dispatch:
jobs:
  release:
    name: Register Package
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/commit-comment@v3
        with:
          body: |
            @JuliaRegistrator register

            This comment was generated with `commit-comment` and [`Tests.yml`](/.github/workflows/Tests.yml).
